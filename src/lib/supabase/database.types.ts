// Placeholder until generated from the real schema via:
//   supabase gen types typescript --local > src/lib/supabase/database.types.ts
// Keeping an explicit placeholder (rather than `any` scattered around call
// sites) makes it obvious this file is a stand-in, not hand-maintained.
export type Database = Record<string, unknown>;
